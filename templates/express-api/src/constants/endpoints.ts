// import { apiVersion } from './api-version';

export const endPoints = {
  tasks: `/tasks`,
  taskLists: `/task-lists`
};
// export const endPoints = {
//   tasks: `/api/v${apiVersion}/tasks`,
//   taskLists: `/api/v${apiVersion}/task-lists`
// };
